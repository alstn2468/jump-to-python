'''
파이썬은 다음처럼 동일한 값에 여러개의 변수를 선언할 수 있다.
아래와 같이 a, b 변수를 선언 한 후 a의 첫번째 요소값을 변경하면
b의 값은 어떻게 될까? 그리고 이런 결과가 나오는 이유에 대해서 설명해 보자.
'''

a = b = [1, 2, 3]
a[1] = 4
print(b) # [1, 4, 3]

# a와 b 모두 [1, 2, 3]이라는 동일한 리스트 객채를 가리키고 있기 때문이다.
